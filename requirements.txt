# CUDA 12.8 builds of PyTorch. They also run on machines without an NVIDIA GPU;
# removing this line installs the smaller CPU-only build instead.
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0
sentence-transformers==6.0.1
transformers==5.17.0
numpy==2.5.3
rank-bm25==0.2.2
httpx==0.28.1
